# Archived Bridge Configs

This folder stores configuration files kept for reference only.

Files here are not part of the active runtime default configuration.

Active runtime registry:

- `bridge/config/instrument_registry.marimba.v1.json`
